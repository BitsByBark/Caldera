{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for main window IPC and dialog access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
